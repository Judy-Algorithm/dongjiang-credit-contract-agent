# 本地流程测试资料

本目录仅包含合成数据，不对应真实客户，也不会在前端自动生成案件。用户必须按正式业务顺序上传：

1. 业务经办人发起信审，上传 `synthetic-credit-profile.txt`。
2. 信用审批人审批授信，使授信结果正式生效。
3. 切回业务经办人，上传 `standard-tkp-contract.txt`。
4. 查看合同 Agent 自动读取正式授信后的额度、账期与风险校验结果。

为了演示五角色权限，建议先创建系统管理员和四个业务角色账号，再由管理员使用“切换账户”完成演示。
